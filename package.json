{
  "name": "behodler",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ arrenv/behodler",
    "create-local": "graph create --node http://localhost:8020/ arrenv/behodler",
    "remove-local": "graph remove --node http://localhost:8020/ arrenv/behodler",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 arrenv/behodler"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
